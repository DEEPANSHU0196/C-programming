#include<stdio.h>
int fib(int );
int main()
{
    int i,terms;
    printf("Enter the value of terms");
    scanf("%d",&terms);
    for (i=0;i<terms;i++)
{
    printf("%d",fib(i));
}
}
int fib (int a)
{
    if (a<=0)
    return a;
    else 
    return fib(a-1)+fib(a-2);
}
