#include <stdio.h>
int main()
{ int a;
//input the value a 
  printf("write the no.");
  scanf("%d",&a);
  if ((a%5==0)&&(a%11==0))
  printf("no. is divisible by 5 & 11");
  else 
  printf ("the no. is not divisible by 5 and 11 ");
  return 0;
}
