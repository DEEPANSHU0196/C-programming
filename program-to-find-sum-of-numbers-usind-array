#include<stdio.h>
int main()
{
    int X[10],i,sum;
    for (i=0;i<=9;i++)
        scanf("%d",&X[i]);
    for(i=0;i<=9;i++)
        printf ("%d\n",X[i]);
        sum=0;
    for(i=0;i<=9;i++)
        sum=sum+X[i];
        printf("sum=%d\n",sum);
    return 0;
}
